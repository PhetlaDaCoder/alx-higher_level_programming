-- Inserts a new row into the table first_table in MySQL server.
INSERT INTO first_table (id, name) VALUES (89, "Best School");

-- Counts the rowsin first_table.
SELECT COUNT(*) FROM first_table WHERE id=89;
